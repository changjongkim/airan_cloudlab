# Historical documents

이 디렉터리는 과거 가설, 임시 계획, 당시의 final report를 보존한다. 파일명에 `FINAL`, `MASTER`, `NEXT`가 있어도 현재 authoritative 상태를 의미하지 않는다.

현재 연구 판단은 [`../current/`](../current/) 문서만 사용한다. 과거 figure 링크는 저장소 루트의 `charts/`와 `results/`를 계속 가리키도록 유지했다.
