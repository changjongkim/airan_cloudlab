# Root artifact archive

- `root_logs/`: 초기 루트 수준 실행 로그
- `root_markers/`: chain 완료 marker
- `monitoring/`: 자동 monitor 스크립트와 stdout
- `early_root_runs/`: `results/` 정착 전 생성된 초기 run 사본

현재 연구 결과는 이 디렉터리가 아니라 `data/README.md`에서 찾는다.
